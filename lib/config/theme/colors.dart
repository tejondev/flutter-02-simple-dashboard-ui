import 'package:flutter/material.dart';

// TextStyle
kTitleStyle() {
  return const TextStyle(
      color: Colors.white, fontSize: 18, fontWeight: FontWeight.bold);
}

kTitleGreenStyle() {
  return const TextStyle(
      color: kGreenColor, fontSize: 18, fontWeight: FontWeight.bold);
}

kSubtitleStyle() {
  return const TextStyle(color: Colors.white, fontSize: 14);
}

kMinSubtitleStyle() {
  return const TextStyle(color: Colors.white, fontSize: 12);
}

// App colors
const kBackgroundColor = Color(0xFFE5F0F4);
const kPrimaryColor = Colors.teal;
const kSecondaryColor = Color(0xFF7BC496);

// Dashboard theme
const kBlueColor = Colors.blue;
const kPurpleColor = Colors.purple;
const kRedColor = Colors.red;
const kGreenColor = Colors.green;
